---
phase: 01-seo-discoverability
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/App.jsx
  - src/components/BeachScene.jsx
  - src/styles/global.css
autonomous: true
requirements:
  - REQ-008
  - REQ-009
  - REQ-012

must_haves:
  truths:
    - "Page has exactly one h1 element containing the developer name"
    - "The main content area is wrapped in a semantic <main> element"
    - "The navigation bar uses a semantic <nav> element"
    - "The 3D canvas has an aria-label for screen readers"
  artifacts:
    - path: "src/App.jsx"
      provides: "Semantic main wrapper and visually-hidden h1"
      contains: "sr-only"
    - path: "src/components/BeachScene.jsx"
      provides: "Canvas with aria-label and role=img"
      contains: "aria-label"
    - path: "src/styles/global.css"
      provides: "sr-only utility class"
      contains: ".sr-only"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/BeachScene.jsx"
      via: "BeachScene rendered inside <main>"
      pattern: "<main"
    - from: "src/styles/global.css"
      to: "src/App.jsx"
      via: "sr-only class used by h1"
      pattern: "sr-only"
---

<objective>
Add semantic HTML structure (main, h1) to App.jsx, aria-label to the 3D Canvas, and the sr-only utility class to global.css.

Purpose: Enable search engine crawlers and screen readers to understand the page structure despite the 3D canvas-based UI.
Output: Semantic HTML wrappers in App.jsx, accessible Canvas in BeachScene.jsx, sr-only utility in global.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-discoverability/01-RESEARCH.md
@src/App.jsx
@src/components/BeachScene.jsx
@src/components/Navbar.jsx
@src/styles/global.css

<interfaces>
<!-- Key component signatures the executor needs -->

From src/App.jsx:
```jsx
function PortfolioContent() {
  // Returns: <div style={styles.container}> wrapping BeachScene, Navbar, PostIt, hint div
}
export default function App() {
  // Returns: <ThemeProvider><PortfolioContent /></ThemeProvider>
}
```

From src/components/BeachScene.jsx (line 579):
```jsx
<Canvas
  camera={{ position: [0, 2.2, 6], fov: 60 }}
  style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', zIndex: 0 }}
  gl={{ antialias: true, alpha: false }}
>
```

From src/components/Navbar.jsx (line 17):
```jsx
<nav style={styles.nav}>
  <!-- Already uses <nav> — no change needed here -->
</nav>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sr-only class to global.css</name>
  <files>src/styles/global.css</files>
  <read_first>src/styles/global.css</read_first>
  <action>
Append the following `.sr-only` utility class to the END of `src/styles/global.css` (REQ-008 support):

```css
/* Screen-reader only — visually hidden but accessible to crawlers and assistive tech */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

Do NOT modify any existing CSS rules. Only append this new class at the end of the file.
  </action>
  <verify>
    <automated>grep -c "sr-only" src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `.sr-only` class exists in global.css (grep: `grep ".sr-only" src/styles/global.css`)
    - Contains `position: absolute` (grep: `grep "position: absolute" src/styles/global.css`)
    - Contains `clip: rect(0, 0, 0, 0)` (grep: `grep "clip: rect" src/styles/global.css`)
    - All existing CSS rules remain unchanged (existing :root, [data-theme="dark"], *, html/body selectors still present)
  </acceptance_criteria>
  <done>sr-only utility class appended to global.css without disturbing existing styles</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic HTML to App.jsx and aria-label to BeachScene Canvas</name>
  <files>src/App.jsx, src/components/BeachScene.jsx</files>
  <read_first>src/App.jsx, src/components/BeachScene.jsx, src/components/Navbar.jsx</read_first>
  <action>
**In src/App.jsx** (REQ-008):

1. Wrap the content inside `PortfolioContent`'s outer `<div>` with a `<main>` element. The `<main>` should wrap BeachScene, Navbar, PostIt, and the hint div — everything inside the container div.

2. Add a visually-hidden `<h1>` as the first child inside `<main>`:
   ```jsx
   <h1 className="sr-only">Nino Rey Cabiltes — Full Stack Developer</h1>
   ```

The updated PortfolioContent return should look like:
```jsx
return (
  <div style={styles.container}>
    <main>
      <h1 className="sr-only">Nino Rey Cabiltes — Full Stack Developer</h1>

      {/* 3D Beach Background with integrated laptop */}
      <BeachScene navTarget={navTarget} onTerminalActivity={handleActivity} />

      {/* Navbar */}
      <Navbar onNavigate={handleNavigate} onActivity={handleActivity} />

      {/* PostIt note */}
      <PostIt />

      {/* Hint */}
      <div style={styles.hint}>
        Click the laptop to focus, drag to orbit
      </div>
    </main>
  </div>
);
```

Do NOT change the styles object, imports, state logic, or any other part of App.jsx. Only add `<main>` wrapper and `<h1>`.

**In src/components/BeachScene.jsx** (REQ-009):

Add `aria-label` and `role` attributes to the `<Canvas>` component on line 579. Change from:
```jsx
<Canvas
  camera={{ position: [0, 2.2, 6], fov: 60 }}
  style={{...}}
  gl={{ antialias: true, alpha: false }}
>
```

To:
```jsx
<Canvas
  camera={{ position: [0, 2.2, 6], fov: 60 }}
  style={{...}}
  gl={{ antialias: true, alpha: false }}
  aria-label="Interactive 3D beach scene portfolio for Nino Rey Cabiltes"
  role="img"
>
```

Do NOT change anything else in BeachScene.jsx — only add the two attributes to the Canvas element.

**REQ-012 note:** The research confirms there are no `<img>` tags in this app. The only image file (`ascii-art.png`) is referenced as text content in personalDetails.json, not as an `<img>` element. The Three.js canvas renders all visuals programmatically. REQ-012 (lazy loading) is satisfied by the canvas aria-label approach — there are no images to lazy-load. The Canvas itself is rendered by React and does not need `loading="lazy"`.
  </action>
  <verify>
    <automated>grep -c "<main>" src/App.jsx && grep -c "sr-only" src/App.jsx && grep -c "aria-label" src/components/BeachScene.jsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains `<main>` element (grep: `grep "<main>" src/App.jsx` returns match)
    - App.jsx contains `<h1 className="sr-only">` (grep: `grep 'sr-only' src/App.jsx` returns match)
    - App.jsx h1 contains exact text (grep: `grep "Nino Rey Cabiltes" src/App.jsx` returns match)
    - BeachScene.jsx Canvas has aria-label (grep: `grep 'aria-label' src/components/BeachScene.jsx` returns match)
    - BeachScene.jsx Canvas has role="img" (grep: `grep 'role="img"' src/components/BeachScene.jsx` returns match)
    - Navbar.jsx still uses `<nav>` (grep: `grep '<nav' src/components/Navbar.jsx` — unchanged)
    - `pnpm build` succeeds (no JSX syntax errors from the changes)
  </acceptance_criteria>
  <done>App.jsx has semantic main + h1, BeachScene Canvas has aria-label + role. Build succeeds. REQ-008, REQ-009, REQ-012 satisfied.</done>
</task>

</tasks>

<verification>
- `grep "<main>" src/App.jsx` matches
- `grep "sr-only" src/App.jsx` matches (h1 uses the class)
- `grep ".sr-only" src/styles/global.css` matches (class defined)
- `grep "aria-label" src/components/BeachScene.jsx` matches
- `grep 'role="img"' src/components/BeachScene.jsx` matches
- `pnpm build` succeeds
- `grep -rn "<nav" src/components/Navbar.jsx` confirms nav element still present
</verification>

<success_criteria>
1. Exactly one `<h1>` on the page containing "Nino Rey Cabiltes — Full Stack Developer" (sr-only)
2. `<main>` wraps all page content in App.jsx
3. `<nav>` already present in Navbar.jsx (no change needed — confirmed)
4. Canvas has `aria-label="Interactive 3D beach scene portfolio for Nino Rey Cabiltes"` and `role="img"`
5. `.sr-only` class in global.css properly hides the h1 visually
6. `pnpm build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-discoverability/01-03-SUMMARY.md`
</output>
