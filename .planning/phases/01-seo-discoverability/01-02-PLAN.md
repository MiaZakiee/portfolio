---
phase: 01-seo-discoverability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/robots.txt
  - public/sitemap.xml
  - public/favicon.ico
  - public/favicon-16x16.png
  - public/favicon-32x32.png
  - public/apple-touch-icon.png
  - public/og-image.png
autonomous: true
requirements:
  - REQ-006
  - REQ-010
  - REQ-011

must_haves:
  truths:
    - "robots.txt is accessible at /robots.txt after build"
    - "sitemap.xml is accessible at /sitemap.xml after build"
    - "Favicon appears in browser tab"
    - "OG image file exists for social media previews"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawl rules for search engines"
      contains: "User-agent"
    - path: "public/sitemap.xml"
      provides: "URL index for search engines"
      contains: "ninocabiltes.dev"
    - path: "public/favicon.ico"
      provides: "Legacy browser favicon"
    - path: "public/apple-touch-icon.png"
      provides: "iOS home screen icon"
    - path: "public/og-image.png"
      provides: "Social media preview image"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive URL"
      pattern: "Sitemap.*ninocabiltes.dev/sitemap.xml"
    - from: "public/sitemap.xml"
      to: "https://ninocabiltes.dev"
      via: "loc element"
      pattern: "<loc>https://ninocabiltes.dev/</loc>"
---

<objective>
Create all static public/ assets: robots.txt, sitemap.xml, favicon set, and OG image placeholder.

Purpose: Search engines need robots.txt and sitemap.xml to crawl efficiently. Browsers need favicons. Social platforms need og-image.png.
Output: 7 files in public/ directory, all copied to dist/ root by Vite build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-discoverability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create robots.txt and sitemap.xml</name>
  <files>public/robots.txt, public/sitemap.xml</files>
  <read_first>public/CNAME</read_first>
  <action>
Create `public/robots.txt` with this exact content (REQ-010):

```
User-agent: *
Allow: /
Sitemap: https://ninocabiltes.dev/sitemap.xml
```

Create `public/sitemap.xml` with this exact content (REQ-011):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://ninocabiltes.dev/</loc>
    <lastmod>2026-03-27</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

Both files go in `public/` — Vite copies everything in `public/` to `dist/` root verbatim on build. GitHub Pages serves files at their exact path, so `/robots.txt` and `/sitemap.xml` will be directly accessible.

IMPORTANT: The robots.txt Sitemap URL must be absolute (https://ninocabiltes.dev/sitemap.xml), not relative. The sitemap loc must also be absolute with trailing slash.
  </action>
  <verify>
    <automated>test -f public/robots.txt && test -f public/sitemap.xml && grep "Sitemap: https://ninocabiltes.dev/sitemap.xml" public/robots.txt && grep "ninocabiltes.dev" public/sitemap.xml</automated>
  </verify>
  <acceptance_criteria>
    - public/robots.txt exists
    - robots.txt contains "User-agent: *" (grep: `grep "User-agent: \*" public/robots.txt`)
    - robots.txt contains "Allow: /" (grep: `grep "Allow: /" public/robots.txt`)
    - robots.txt contains "Sitemap: https://ninocabiltes.dev/sitemap.xml" (grep exact match)
    - public/sitemap.xml exists
    - sitemap.xml contains XML declaration (grep: `grep '<?xml' public/sitemap.xml`)
    - sitemap.xml contains loc with full URL (grep: `grep '<loc>https://ninocabiltes.dev/</loc>' public/sitemap.xml`)
    - sitemap.xml contains lastmod 2026-03-27 (grep: `grep '2026-03-27' public/sitemap.xml`)
    - After `pnpm build`: `test -f dist/robots.txt && test -f dist/sitemap.xml`
  </acceptance_criteria>
  <done>robots.txt and sitemap.xml exist in public/, contain correct absolute URLs, and appear in dist/ after build</done>
</task>

<task type="auto">
  <name>Task 2: Generate favicon set and OG image placeholder</name>
  <files>public/favicon.ico, public/favicon-16x16.png, public/favicon-32x32.png, public/apple-touch-icon.png, public/og-image.png</files>
  <read_first>public/ascii-art.png</read_first>
  <action>
Generate the favicon set and OG image placeholder for REQ-006. Since no design tool or image editor is available in the CLI, use Node.js canvas or a programmatic approach.

**Option A (preferred): Use a minimal SVG-to-PNG pipeline with sharp or canvas.**

If `sharp` or `canvas` npm packages are not available and cannot be installed, use this alternative:

**Option B: Create minimal valid PNG files programmatically using Node.js Buffer.**

Create a simple Node.js script (do NOT commit it — run and delete) that generates:

1. `public/favicon.ico` — 32x32 ICO file. Can be a simple solid-color square with "N" or terminal-themed icon.
2. `public/favicon-16x16.png` — 16x16 PNG
3. `public/favicon-32x32.png` — 32x32 PNG
4. `public/apple-touch-icon.png` — 180x180 PNG
5. `public/og-image.png` — 1200x630 PNG (social media preview)

The favicons should be simple and recognizable — a solid dark background (#1e1e2e, matching terminal theme) with a light colored "N" letter or terminal prompt symbol. These are placeholder-quality — the developer can replace them with custom designs later.

If programmatic image generation proves too complex without dependencies, create minimal valid PNG files (even single-color squares) as placeholders. The critical requirement is that the files EXIST at the correct paths and are valid image files that browsers can load without errors.

After generating, verify each file exists and is a valid image (not 0 bytes).

Delete the generation script after running it — do not commit build scripts for one-time assets.
  </action>
  <verify>
    <automated>test -f public/favicon.ico && test -f public/favicon-16x16.png && test -f public/favicon-32x32.png && test -f public/apple-touch-icon.png && test -f public/og-image.png && test -s public/favicon.ico && test -s public/og-image.png</automated>
  </verify>
  <acceptance_criteria>
    - public/favicon.ico exists and is non-empty (`test -s public/favicon.ico`)
    - public/favicon-16x16.png exists and is non-empty (`test -s public/favicon-16x16.png`)
    - public/favicon-32x32.png exists and is non-empty (`test -s public/favicon-32x32.png`)
    - public/apple-touch-icon.png exists and is non-empty (`test -s public/apple-touch-icon.png`)
    - public/og-image.png exists and is non-empty (`test -s public/og-image.png`)
    - No generation script left behind (no temp .js files in root)
    - After `pnpm build`: `ls dist/favicon.ico dist/og-image.png` succeeds
  </acceptance_criteria>
  <done>All 5 image files exist in public/, are non-empty valid image files, and appear in dist/ after build. No build scripts committed.</done>
</task>

</tasks>

<verification>
- `ls public/robots.txt public/sitemap.xml public/favicon.ico public/favicon-16x16.png public/favicon-32x32.png public/apple-touch-icon.png public/og-image.png` — all 7 files exist
- `pnpm build` succeeds
- `ls dist/robots.txt dist/sitemap.xml dist/favicon.ico dist/og-image.png` — files copied to build output
- `grep "ninocabiltes.dev" public/robots.txt public/sitemap.xml` — both reference the correct domain
</verification>

<success_criteria>
1. robots.txt in public/ with correct User-agent, Allow, and Sitemap directives
2. sitemap.xml in public/ with single URL entry for https://ninocabiltes.dev/
3. All 4 favicon files exist and are non-empty valid images
4. og-image.png exists at 1200x630 (or valid placeholder)
5. `pnpm build` succeeds and all files appear in dist/
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-discoverability/01-02-SUMMARY.md`
</output>
